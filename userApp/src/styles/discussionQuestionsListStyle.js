import css from "styled-jsx/css";
import variables from "$/styles/variables";

export const discussionQuestionsListStyle = css.global`
	.questions__list {
		// width: inherit;
	}

	.questions__list ul {
		list-style: none;
		padding: 0;
	}
`;

export default discussionQuestionsListStyle;
