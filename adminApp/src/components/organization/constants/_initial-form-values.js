// TODO: the component mutates only admins[0] investigate later on
// could be related to library
const initialFormValues = {
  form: {
    organizationName: '',
    admins: [
      {
        key: 0,
        value: ''
      }
    ]
  }
}

export default initialFormValues
